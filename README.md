Qt 6 UI Lockout with Duration by Modal Dialog Example
===

Summary
---
In this example of a Qt 6 interface witten in C++, all select UI events trigger a UI lock-out for a fixed — in this case, hard-coded — duration.
